syntax = "proto3";

package games.lensmanagement;

message SCGamesLensManagementListLensesUsedRequest {
	int32 limit = 1;
	string cursor = 2;

	enum SCGamesLensManagementPlatform {
		UNSET = 0;
		CAMKIT = 1;
	}

	SCGamesLensManagementPlatform platform = 3;
}
