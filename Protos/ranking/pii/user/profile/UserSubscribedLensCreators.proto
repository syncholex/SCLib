syntax = "proto3";

package ranking.pii.user.profile;

import "ranking/pii/user/profile/LensCreatorSubscribeState.proto";

message UserSubscribedLensCreators {
	string userId = 1;
	ranking.pii.user.profile.LensCreatorSubscribeState subscribedLensCreators = 2;
}
