syntax = "proto3";

package ranking.pii.user.profile;

import "ranking/pii/user/profile/LiveSubscribeDetail.proto";

message UserSubscribedLives {
	string userId = 1;
	ranking.pii.user.profile.LiveSubscribeDetail subscribedDetails = 2;
}
