syntax = "proto3";

package ranking.pii.user.profile;

import "ranking/pii/user/profile/PublisherSubscribeDetail.proto";

message UserSubscribedPublishers {
	string userId = 1;
	ranking.pii.user.profile.PublisherSubscribeDetail subscribedPublishers = 2;
}
