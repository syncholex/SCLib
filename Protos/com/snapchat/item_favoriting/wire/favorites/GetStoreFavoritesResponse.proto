syntax = "proto3";

package com.snapchat.item_favoriting.wire.favorites;

import "com/snapchat/item_favoriting/wire/favorites/FavoriteItem.proto";

message GetStoreFavoritesResponse {
	bytes paginationToken = 1;
	repeated com.snapchat.item_favoriting.wire.favorites.FavoriteItem favoritedItemsArray = 2;
}
