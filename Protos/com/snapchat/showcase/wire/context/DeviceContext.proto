syntax = "proto3";

package com.snapchat.showcase.wire.context;

message DeviceContext {

	enum DeviceType {
		DEVICE_TYPE_UNSET = 0;
		ANDROID = 1;
		IOS = 2;
		WEB = 3;
	}

	DeviceType deviceType = 1;
	string appVersion = 2;
}
