syntax = "proto3";

package snapchat.valis;

message SCVSLocationPermission {

	enum SCVSLocationPermission_Type {
		UNSET = 0;
		NEVER = 1;
		ONCE = 2;
		WHILE_USING = 3;
		ALWAYS = 4;
	}

	SCVSLocationPermission_Type type = 1;
	bool precise = 2;
}
