syntax = "proto3";

package snapchat.map.actionmoji;

message SCMAGetActionmojiAssetsRequest {

	enum SCMAAssetType {
		ASSET_TYPE_UNSET = 0;
		ASSET_TYPE_CAR = 1;
		ASSET_TYPE_PET = 2;
	}

	repeated SCMAAssetType assetTypesArray = 1 [packed=true];
}
