syntax = "proto3";

package snapchat.auth.passkey.api.external;

message ListPasskeysResult {

	enum ListPasskeysResult_Enum {
		UNSET = 0;
		SUCCESS = 1;
	}

	ListPasskeysResult_Enum statusCode = 1;
	string humanReadableLocalizedMessage = 2;
}
