syntax = "proto3";

package snapchat.cdp.prefetcher;

import "snapchat/cdp/prefetcher/BackgroundPrefetchConfig.proto";

message FeaturedStoryBackgroundPrefetchConfig {
	snapchat.cdp.prefetcher.BackgroundPrefetchConfig prefetchConfig = 1;
	int32 mediaCount = 2;
	int32 prefetchTime = 3;
}
