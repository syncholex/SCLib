syntax = "proto3";

package snapchat.grid;

message SDMPaths {
	repeated uint32 deltaLengthsArray = 1 [packed=true];
	repeated sint32 xValuesArray = 2 [packed=true];
	repeated sint32 yValuesArray = 3 [packed=true];
}
